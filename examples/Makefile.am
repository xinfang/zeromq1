if BUILD_EXCHANGE
DIR_EXCHANGE = exchange
endif

if BUILD_CAMERA
DIR_CAMERA = camera
endif

if BUILD_CHAT
DIR_CHAT = chat
endif

if BUILD_BUTTERFLY
DIR_BUTTERFLY = butterfly
endif

SUBDIRS = $(DIR_EXCHANGE) $(DIR_CAMERA) $(DIR_CHAT) $(DIR_BUTTERFLY)
DIST_SUBDIRS = exchange camera chat butterfly
